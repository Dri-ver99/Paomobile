const identifier = document.getElementById(" regEmail\).value.trim(); const password = document.getElementById(\regPassword\).value; const name = identifier.includes(\@\) ? identifier.split(\@\)[0] : \User \ + identifier.slice(-4); await saveUserToFirestore(cred.user, identifier.includes(\@\) ? \email_registration\ : \phone_registration\); window.finishLoginWithUser = () => finishLogin(name); hideLoading(); document.getElementById(\registerView\).style.display = \none\; document.getElementById(\successView\).style.display = \block\; setTimeout(() => { if (document.getElementById(\successView\).style.display !== \none\ && window.finishLoginWithUser) window.finishLoginWithUser(); }, 3000);